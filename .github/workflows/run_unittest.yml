name: CI

on:
  push:
    branches: [ '*' ]

jobs:
  gpio_driver:
    runs-on: macos-latest
    defaults:
      run:
        shell: bash
    steps:
    - uses: actions/checkout@v2
      with:
          repository: smuuze/rpi_control_frmwrk
          path: ./rpi_control_frmwrk
    - uses: actions/checkout@v2
      with:
          repository: smuuze/rpi_control_rp2040
          path: ./rpi_control_rp2040
    - name: unittest
      run: |
        ls
        cd rpi_control_frmwrk
        ls
        cd rpi_control_frmwrk/unittest
        ls
        chmod +x make_unittest.sh
