name: CI

on:
  push:
    branches: [ '*' ]

jobs:
  unittest:
    runs-on: macos-latest
    defaults:
      run:
        shell: bash
        working-directory: unittest
    steps:
    - uses: actions/checkout@v2
    - name: build unittest
      run: ./make_unittest.sh
